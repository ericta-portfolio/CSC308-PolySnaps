import * as React from "react";
import { IModalProps } from "./types";
export declare class Modal extends React.PureComponent<IModalProps, {}> {
    static defaultProps: Partial<IModalProps>;
    render(): React.ReactNode;
}
