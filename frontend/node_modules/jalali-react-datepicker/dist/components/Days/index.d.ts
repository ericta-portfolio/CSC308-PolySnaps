import * as React from "react";
import { IDaysProps } from "./types";
export declare class Days extends React.PureComponent<IDaysProps> {
    static defaultProps: Partial<IDaysProps>;
    render(): React.ReactNode;
}
